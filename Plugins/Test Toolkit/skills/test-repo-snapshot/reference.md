# Output template (bundled file test)

```
<full_name> - <description>
Stars: <stargazers_count> | Default branch: <default_branch> | Last push: <pushed_at>
```

If an agent can quote this template, bundled-file retrieval via `get_skill` (with `file: reference.md`) works.
