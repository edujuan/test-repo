---
name: test-shared-linking
description: Test skill. Shared skill with no tools, used to test linking a skill into a plugin (T17).
metadata:
  version: "0.1.0"
  owner: "Testing"
  lifecycle: active
---
# Shared linking fixture

When invoked, reply exactly: `test-shared-linking is reachable`.

If an agent in the Test Toolkit plugin can load this skill, the plugin link AND the read grant on this folder are both in place.
